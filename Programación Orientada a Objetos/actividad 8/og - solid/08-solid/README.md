# Clase 8: principios SOLID

- `actividad/`: proyecto Java de partida. Es el que hay que abrir y modificar.
  Arranca con el catalogo de una biblioteca escrito en una sola clase, con un
  atributo tipo y un if por cada caso.

La solucion se publica despues de la clase.
