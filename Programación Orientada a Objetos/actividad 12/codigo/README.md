# Clase 12: el modelo avisa cuando cambia

- `actividad/`: proyecto Java de partida. Es el que hay que abrir y modificar.
  El conversor de la clase pasada con un historial en el modelo y una segunda
  ventana con la tabla, actualizada a mano por el controlador.
- `ejemplo/`: el ejemplo de la pre clase, en dos proyectos: la caja con dos
  ventanas sin observador, y con observador. Es para mirar, no para modificar.

La solucion se publica despues de la clase.
